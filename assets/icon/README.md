# App icon

Drop your launcher artwork here as **`app_icon.png`** (square, 1024×1024, no
transparency for iOS). Then generate every Android + iOS size with:

```
flutter pub run flutter_launcher_icons
```

Config lives under `flutter_launcher_icons:` in `pubspec.yaml`.
